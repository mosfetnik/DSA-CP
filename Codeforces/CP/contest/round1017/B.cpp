#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int n, m, l, r;
    cin >> n >> m >> l >> r;

    cout <<n+l << " "<< 
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    long long t;
    cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}